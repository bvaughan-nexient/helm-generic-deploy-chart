{{- if (ne .Values.service nil) }}
{{- include "helm-library.service" (list . "chart.service") -}}
{{- end }}
{{- define "chart.service" -}}
# configuration to be merged with manifest generated by the library
{{- end -}}